<h1><%= @patient.name %></h1>
<h4><%= @patient.age %></h4>

<ul>
  <% @patient.appointments.each do |appt| %>
  <li><%= appt.appointment_datetime.strftime("%B %e, %Y at %H:%M") %></li>
  <% end %>
</ul>
